@AGENTS.md

## Claude Code notes

- Treat these instructions as project-level guidance.
- For all UI work, treat `docs/Atlas_UI_Rework_Implementation_Plan.md` as the
  mandatory directive. The legacy UI must not be used as the rework's visual,
  layout, navigation, component, or interaction reference.
- Prefer planning before broad edits.
- For rework UI tasks, inspect the relevant `ui/AGENTS.md` and the rework plan;
  do not route the task through the legacy UI polish checklist.
- Keep responses and implementation summaries concise and action-oriented.
